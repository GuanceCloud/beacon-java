name: Build

on:
  push:
    branches:
      - main
      - release/*
  workflow_dispatch:

permissions:
  contents: read

jobs:
  common:
    if: github.repository == 'open-telemetry/opentelemetry-java-instrumentation'
    uses: ./.github/workflows/build-common.yml
    secrets:
      DEVELOCITY_ACCESS_KEY: ${{ secrets.DEVELOCITY_ACCESS_KEY }}

  test-latest-deps:
    # release branches are excluded
    # because any time a new library version is released to maven central it can fail
    # which requires unnecessary release branch maintenance, especially for patches
    if: github.repository == 'open-telemetry/opentelemetry-java-instrumentation' && (!startsWith(github.ref_name, 'release/'))
    uses: ./.github/workflows/reusable-test-latest-deps.yml
    secrets:
      DEVELOCITY_ACCESS_KEY: ${{ secrets.DEVELOCITY_ACCESS_KEY }}

  muzzle:
    # release branches are excluded
    # because any time a new library version is released to maven central it can fail
    # which requires unnecessary release branch maintenance, especially for patches
    if: github.repository == 'open-telemetry/opentelemetry-java-instrumentation' && (!startsWith(github.ref_name, 'release/'))
    uses: ./.github/workflows/reusable-muzzle.yml

  lint:
    # release branches are excluded because the README.md javaagent download link has to be updated
    # on release branches before the release download has been published
    if: github.repository == 'open-telemetry/opentelemetry-java-instrumentation' && (!startsWith(github.ref_name, 'release/'))
    uses: ./.github/workflows/reusable-lint-check.yml
